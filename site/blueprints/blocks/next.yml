name: Next
label: "{{title}}"
icon: 💡
fields:
  header: fields/headlineGroup
  link:
    label: Link
    type: structure
    fields:
      href:
        label: href
        type: text
      title:
        label: Title
        type: text
  image:
    label: Stage Image
    type: files
    multiple: false
    query: site.find("image").files
  divider:
    type: line
