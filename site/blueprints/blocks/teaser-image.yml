name: Teaser Image
label: "{{title}}"
icon: 🎤
fields:
  header: fields/headlineGroup
  classes:
    label: Classes
    type: text
  reversed:
    label: Reversed
    type: toggle
  image:
    label: Stage Image
    type: files
    multiple: false
    query: site.find("image").files
  divider:
    type: line
